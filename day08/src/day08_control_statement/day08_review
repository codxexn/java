8회차 앞부분 문제풀이

day05-test-test05
System.out.println("평균은 " + avg + "점이고, 학점은 " + grade + "학점 입니다.");
System.out.printf("평균은 %10.2f점이고, 학점은 %c학점 입니다.", avg, grade);

Q. 근데 10.2는 뭐지?
(어떻게 출력을 해줄거냐 는 의미)
10은 앞에 띄워줄 공백수
숫자는 상관없음
10 안써주면 공백 없음!
.2 는 소수점 몇자리까지 출력할 거냐?

%d 정수형 출력
%s 문자열 출력
%f 실수형 출력
%c 문자형 출력
%n 엔터
%b boolean 출력


if (시작점, 끝점, 반복할 것)
컨티뉴를 만나서 올라가면 자동으로 다음턴
자동으로 ++
쭉 카운팅이 되고 있는데 continue 로 
조건만 빼주는 거
= 처음에는 시작점, 끝, 조건
다음에는 +를 읽고 끝을 읽음
증가하고 비교하고 돈다.
시작점, 초기문은 처음에만 읽는다!

while 자동 증가가 아니라
우리가 작성한 조건에 충족해야 ++
if 로 continue 를 걸어주면 값이 ++ 안됨
증가할 조건을 우리가 만들어 주는 것
= 처음 보는 건 비교밖에 없다( ) 안에 값
증가문을 만나야지만 증가를 한다.
어디에 놓는지에 따라 증가를 할수도 있고
안 할수도 있다.
(카운팅을 안하고 싶은 조건이 있다고 하면 while을 사용하고 continue 를 사용하고 카운팅을 안해주면 된다.)




배열 : 값의 배열
값을 묶어서 군집화해서 관리를 하는 게 더 좋은 상황이 많다. 그래서 존재함.
참조변수







